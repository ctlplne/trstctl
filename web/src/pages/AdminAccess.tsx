/** The route implementation is isolated so the answer-first access journey is
 * reviewable without mixing it into the other administration surfaces. */
export { AdminAccess } from "@/pages/AdminAccessJourney";
